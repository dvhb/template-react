branches:
  only:
    - master
    - next
    - /^feature.*$/
    - /^\d+\.(\d+|x)(\.x)?$/

jobs:
  include:
    - stage: release
      node_js: lts/*
      install:
        - travis_retry npm ci
      script:
        - npx semantic-release
